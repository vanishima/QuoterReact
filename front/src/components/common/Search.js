import React from "react";
// import PropTypes from "prop-types";

const Search = (props) => {
  return (
    <div className="mb-3">
      <form action="">
        <input className="form-control search" type="text" placeholder="Search quotes"></input>
      </form>
    </div>
  );
};

// Search.propTypes = {};

export default Search;
